# 함수의 정의
# 함수의 타입 (4가지)
    # 매개변수 존재 O, 반환값 x
    # 매개변수 존재 O, 반환값 O
    # 매개변수 존재 x, 반환값 O
    # 매개변수 존재 x, 반환값 x

# 매개변수 종류
    # 기본 매개변수  : 뒤에서 부터 채운다. 
    # 가변 매개변수(매개변수를 동적으로 사용) : 함수 내부에서 리스트로 저장. * 키워드 사용 
    # 키워드 매개변수 --> 딕셔너리
    # 가변 키워드 매개변수

# 함수 생성
    # def 함수명( 옵션 ):
        # 내용
        # return (옵션)
# 함수 호출
    # 함수 이름(옵션)
    # 함수는 호출이 끝나면 해당 위치에 반환값이 있으면 가지고 온다. 
    # result = myFunc()
